{
  "buildCommand": "cd net && npm install && npm run build",
  "outputDirectory": "net/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
    
  ]
}
